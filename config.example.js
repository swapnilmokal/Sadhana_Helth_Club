/* ============================================================
   config.js  —  साधना हेल्थ अँड न्यूट्रिशन सेंटर
   1. In the GitHub repository, create a NEW file named  config.js  (same folder as README.md).
   2. Copy everything from this file into it.
   3. Replace the URL below with your Google Apps Script "Web app" URL, then Commit.
   Because config.js is not part of the update zip, later app updates will NOT overwrite it.
   While the URL is not set, both apps run in demo mode (data only in that browser).
   ============================================================ */
window.SADHANA_CONFIG = {
  API_URL: 'PASTE_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE'
};
